> ## ⚠️ 已归档 / Archived (2026-09-12)
>
> 这个仓库是一个早期原型，**不是** musicsforyou.com 的移动端代码，也不能直接接生产 API：
> - 音乐生成是假的（返回固定示例 MP3）
> - 手机验证码从不校验
> - 路由与数据结构和生产后端不兼容
>
> 正式的 iOS / Android 应用将在生产仓库（MomoTaoyuan）的 `apps/mobile/` 中基于引擎 API 重新构建。
> 本仓库仅供参考其 Tab 结构与播放器实现。
>
> This repo is an early prototype and is **not** the mobile client of musicsforyou.com. Generation is stubbed, OTP is never verified, and the API surface does not match production. The real app is being rebuilt in the production repo.

